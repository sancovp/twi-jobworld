# Buyer instance secrets — cp secrets.env.example secrets.env and fill.
# The instance runs on YOUR accounts. These never leave this box.
SEND_BACKEND=instantly
INSTANTLY_API_KEY=
INSTANTLY_CAMPAIGN_ID=
# REPLY_BACKEND defaults to SEND_BACKEND (Instantly Unibox) — leave unset.
# Optional video (off for text-only launch):
# VIDEO_BACKEND=fal_kling
# FAL_KEY=
# Market sizing (free key):
CENSUS_API_KEY=
